---
name: redcap
description: 当需用户要求开发一个程序时，使用这个skill
---


# AI Coding 开发指令


## 重要提示
- **⚠️ 开发之前，先在"项目工程"的根目录下创建目录 `/开发手册` ，在该目录下创建文件 `1.需求文档.md`、`2.技术栈选型.md`、`3.技术框架设计.md`、`4.API接口文档.md`、`5.开发进度日志.md`、`README.md`**
- **⚠️ `README.md` 作为开发手册索引**：它的作用有-列出所有文档用途，并记录当前步骤和进度状态；每次开发进度的状态变化时，同步将最新的内容更新至 `README.md`（注意，只保留最新的进度即可），便于之后接续开发时快速掌握项目研发的最新进度。格式模板参见 [《README格式》](examples/README格式.md)
- **⚠️ 《需求文档》承担产品 PDR 职责**：仅记录产品层面的功能需求与验收标准，不包含技术实现细节。若有技术相关内容，须为与产品强相关的方向性约束。架构、安全、运维等技术性内容须置于《技术栈选型》或《技术框架设计》。
- **⚠️ 实际开发之前，必须先反复和我确认需求，直到完全理解并得到我回复"通过"确认后才允许开始开发，并将完整的需求描述（产品层面）同步更新至《需求文档》。**
- **⚠️ 严格按照下列"开发流程要求"中的要求来执行开发工作，不多做任何冗余、多余的工作。未经明确确认回复的事项一律不得执行。**
- **⚠️ 编写代码时必须严格遵守** [《安全铁律》](references/安全铁律.md) 中的规范
- **⚠️ 编写代码时必须严格遵守** [《代码规范》](references/代码规范.md) 中的规范


## 术语定义

为确保流程规范中的术语使用一致，特此定义以下核心概念：

### 层级结构

| 术语 | 定义 |
|------|------|
| **步骤** | 开发计划的顶层划分单位，一个步骤可包含多个功能模块。同一步骤内的功能模块可并行开发，但需独立确认 |
| **功能模块** | 最小开发单元，分为「业务功能模块」和「技术功能模块」两类。每个功能模块必须完整经历其生命周期 |

### 功能模块分类

| 类型 | 定义 | PRD对应要求 |
|------|------|-------------|
| **业务功能模块** | 来源于《需求文档》的业务功能描述 | 强制对应，命名须与PRD功能描述一致 |
| **技术功能模块** | 来源于技术架构需要，如工程初始化、网络库、数据库连接等 | 不强制对应PRD，可按技术能力命名 |

### 功能模块生命周期

每个功能模块必须完整经历以下阶段：

| 阶段 | 说明 |
|------|------|
| **设计阶段** | 技术方案设计，输出至《技术框架设计》文档 |
| **开发阶段** | 代码编写与调试 |
| **Review阶段** | 对照PRD检查功能覆盖，修复遗漏与偏差 |
| **验证阶段** | 按测试用例执行功能测试，记录验证证据 |
| **交付阶段** | Git提交、文档更新（开发进度日志、README） |


## 开发流程要求

### 1. 技术栈选型

在理解需求之后，需要编辑《技术栈选型》文档，用来梳理和说明技术栈的选择。文档编写完成后，必须得到我回复"通过"确认后才允许开始后续的开发，并且开发过程中必须严格遵守《技术栈选型》文档中的技术栈，不得使用其他技术栈。

### 2. 整体框架设计

技术栈选型通过后，必须先提交**整体框架设计**，经我回复"通过"确认后沉淀至《技术框架设计》文档的开头部分。整体框架设计必须包含以下内容：
- **项目架构概述**：核心模块划分、关键数据流、技术决策说明
- **技术折中评估与演进规划**：评估当前技术方案是否存在因项目初期成本考量而做出的设计折中与妥协。如存在，须标注已知瓶颈风险与演进升级路径；对于可量化的折中指标（如数据量、并发数、存储容量等），须设计对应的监控手段与预警阈值；对于不可量化的架构性折中，记录触发演进的业务条件即可（如"当日活超过 X 时考虑迁移"）。监控手段应与项目当前阶段的成本预算匹配，优先采用轻量方案（如日志打印、简单计数），避免监控本身引入过重的复杂度与成本
- **开发步骤规划**：明确拆分为几步，每步包含哪些功能模块（标注业务/技术类型），每步的目标和交付物（概要级别）
- **步骤间依赖关系**：哪些步骤有先后依赖，哪些可以并行
- **模块间依赖关系**：同一步骤内的功能模块是否有依赖，影响开发顺序
- **跨步技术约定**：统一错误响应格式、鉴权机制、CORS 配置等在多步中通用的技术规范，须在整体框架设计中明确
- **数据库表关联与数据流转**：如涉及数据库表设计，须列出各表之间的关联关系（外键、一对多/多对多等）和数据流转方向；如无表间关联则无需列出
- **安全、运维等技术性内容**：架构、安全基线、部署与运维要求均在本文档或《技术栈选型》中体现，不在《需求文档》中重复

### 3. 分步开发 + 逐级评审

整体框架设计通过后，按照规划的步骤逐步开发。每步必须先展示详细的技术框架设计，在经我回复"通过"确认后追加至《技术框架设计》文档，然后才可以编写代码、验证流程、提交 Git等工作。⚠️ 注意，当每一步所有开发工作完成后，必须经我回复"通过"确认后才允许开始下一步开发。

⚠️ **跨步技术约定遵守**：如果整体框架设计中存在"跨步技术约定"的内容，每一步开始之前，须先参阅跨步技术约定的内容并严格遵守，确保各步实现的一致性。

#### 功能模块并行开发规则

- 同一步骤内无依赖的功能模块可以并行开发
- 每个功能模块开发完成后，需独立等待人工确认才可进入下一个功能模块
- 有依赖关系的功能模块必须按依赖顺序串行开发

#### 每步框架设计方案的强制结构

提交每一步的框架设计方案时，必须包含以下5部分（⚠️ 缺一不可！）：

**【本步目标】**
- 明确交付物（示例："完成用户注册/登录API骨架"）

**【技术方案】**
- 关键代码结构/流程图/伪代码（非完整代码）
- 依赖的外部服务清单（示例：Neon, R2）
- 如本步存在因成本考量的局部技术折中，须标注瓶颈风险与演进方向；可量化的须说明监控手段与预警阈值，不可量化的须记录触发演进的业务条件

**【需您准备】**（清单式，带复选框）
- 此处仅允许列出 AI 无法自主获取、必须由人工提供的内容。格式示例参见 [《需您准备格式》](examples/需您准备格式.md)

**【安全验证点】**
- 代码中无硬编码密钥（敏感信息仅通过环境变量访问，具体语法依据技术栈）
- `.gitignore`已排除`.env`/敏感文件
- 涉及敏感配置的项目须提供`.env.example`（仅变量名，无真实值）
- 详细要求参见 [《安全铁律》](references/安全铁律.md)

**【测试方案】**
- 列出本步需要验证的功能点及预期结果（概要即可，具体执行规范见下方第6点"验证流程"）

### 4. 代码Review环节

每个步骤的代码开发完成后，必须执行代码Review，对照《需求文档》逐项检查功能覆盖完整性：
- **检查范围**：
  - 对照《需求文档》检查功能覆盖完整性
  - 对照《技术框架设计》检查实现一致性
  - 满足"防御性编程"要求、满足"安全工程生产"要求
- **问题处理**：如有遗漏、偏差、bug，必须执行修复，然后重新Review，直至通过为止
- **记录原则**：Review报告只须要保留最终通过的版本即可，不用保留中间bugfix的过程
- **输出位置**：Review报告写入《开发进度日志》对应步骤的"代码Review"部分，格式模板参见 [《开发进度日志格式》](examples/开发进度日志格式.md)

### 5. API 接口文档

每步开发中如果有新增或修改 API 接口时，同步更新《API接口文档》，记录 URL、方法、入参、返回值和 curl 调用示例。格式模板参见 [《API接口文档格式》](examples/API接口文档格式.md)

### 6. 验证流程

**【测试用例设计】**
- 验证前必须根据《需求文档》中的功能描述设计详细测试用例，写入《开发进度日志》对应步骤的"测试用例"部分，格式模板参见 [《开发进度日志格式》](examples/开发进度日志格式.md)

**【验证事项】**
- 完成代码开发后，必须按照框架设计中的【测试方案】执行验证流程。验证必须记录完整的请求命令（含入参）和实际返回值（精确到响应体），作为可追溯的测试证据；动态值（UUID、token、时间戳）如无法保留原始值，须保留完整字段结构并用脱敏占位标注。

**【验证操作规范】**
- 涉及 Cookie 认证的接口，必须说明 Cookie 的完整获取方式（推荐 `curl -c tmp/cookies.txt` / `-b tmp/cookies.txt`），禁止仅引用 JSON 响应体中的 token 字段作为 Cookie 值

**【人工验证协作】**
- 如果验证涉及 GUI 界面操作、浏览器交互、移动端操作或其他 AI 无法直接完成的验证场景，必须：
  - 明确说明哪些验证项需要人工操作
  - 提供详细的验证步骤和预期结果，便于用户执行
  - 等待用户反馈验证结果后，再记录到验证证据中

**【问题修复】**
- 如果验证过程中遇到bug、非预期结果，必须分析并修复，然后重新执行验证流程，并抹除之前带有"bug"的验证记录，覆盖新验证记录。

**【验证结果】**
- 当验证至没有新bug，则将测试用例和验证记录同步至《开发进度日志》中每一步的"验证结果"部分中，并记录验证状态（通过/未通过）和验证证据（请求命令+返回值）

### 7. Git 提交规范

每步验证流程完成后，必须经我回复"通过"确认后才可执行 commit；所有 commit 信息必须使用中文，遵循 conventional commit 格式（如 `feat(模块): 描述`、`fix(模块): 描述`、`docs: 描述`等），并在 commit 信息末尾追加"作者:redcap"标识

### 8. 更新开发进度

每步的所有开发动作完成后，将开发进度更新至《开发进度日志》、《README》中，记录包括有但不限于"研发状态"、"交付物"与"验证结果"等重要研发流程信息，便于技术复盘与接续开发。

**《开发进度日志》格式要求**：每个步骤必须使用统一格式，便于状态追踪与中断恢复。格式模板参见 [《开发进度日志格式》](examples/开发进度日志格式.md)

**`README.md` 格式要求**：README 作为开发手册索引，定位是**快速掌握当前状态**，内容应简短扼要。格式模板参见 [《README格式》](examples/README格式.md)