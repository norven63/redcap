# 任务完成报告：飞书成果汇报、全景图与回复通道治理

**报告日期**：2026-05-09  
**执行者**：Cap（Codex.app）  
**报告版本**：v1.4

---

## 零、先看懂当前局面

### 0.1 当前已完成

- 当前已完成：飞书完成通知的人类可读性已经收口；飞书回复也已接成“安全收件箱”，可以被 RedCap 看到，但不会被当成远程命令直接执行。
- 详情：整体任务全景图已从“当前小任务路线”升级为“RedCap 这次超长主线任务的总览”；飞书回复会进入待处理入口，再由中插需求、人工决策或当前任务合并流程接管。

### 0.2 上一步完成的是

- 上一步完成的是：P2-10 已把飞书从字段堆叠改成六段式汇报，但仍保留了一些偏控制面的表达。

### 0.3 下一步计划做的是

- 下一步计划做的是：继续推进 RedCap 父任务线；如果飞书收件箱出现新回复，先判断它是继续请求、复审请求还是新需求，再合并到任务树，而不是打断主线或自动执行。

### 0.4 整体计划脉络图与当前位置

- 整体计划脉络图是：RedCap 这次超长主线的目标，是从本机 skill 工程演进为更像正式工具的 Agent runtime / CLI / 多层系统；任务流、知识治理、公共库、Forge、LLM-wiki-lite、CLI 产品面、干净工作区验证和飞书治理都已完成一轮落地。
- 当前所在位置：主线仍未整体结束，正式公开发布还未启动；本次完成的是“飞书通知人类可读 + 父任务全景 + 回复安全入口”的一组治理收口。

### 0.5 是否需要 Norven 人工介入

- 人工介入：不需要。
- 说明：本轮不改变飞书账号、不启用后台常驻监听服务、不把你的飞书回复直接当成自动授权。

---

## 一、需求背景

### 1.1 原始问题（用户原文，禁止改写）

> 好的，我看过了，确实效果有很大改善。但是发现目前内容中还是参杂着不少一些AI工作流的名词描述、门禁的概念等非人类可读性、人类易懂性的内容。还是需要再优化一下，我更想通过飞书通知来获悉你到底完成了哪些任务，而不是类似“你是通过哪些节点、门禁、文件操作等达到了哪一个节点”这种偏AI工作流式的表述。

> 另外，调研一下，我是否可以通过飞书给你发指令，比如“我通过回复飞书通知，告知你‘好的，请你们继续下一步任务’这种方式来告知你可以继续往下推进了”，又或者比如“我通过回复飞书通知，告诉你‘这次的任务似乎哪里有问题，需要你们内部再次评审和考虑是否要纠正’”，类似这样的交互方式

> 新版本的飞书回复内容已经基本符合人类可读了。但是发现结构上，确实了“整体任务全景图”的部分，让我不知道目前进展在哪里。另外，这个“整体任务全景图”也要符合人类可读、易懂的准则，不要用“内部节点、门禁、脚本名当作正文主线”的描述

> 你这个“任务全景图”怎么和之前回答我“任务总纲是什么”时的对比，覆盖面窄了好多好多，完全是一个子集（而且还是超小的一个子集）的概念啊。我理解或者我预期的全景任务，应该是从此次的超长任务（尚未结束的主线任务），以及包括中间加入的临时的、新的任务后，全新的调整后的主线任务全景，我表述的够清楚了吗？你是否无法理解的地方？

> 好的，飞书通知的能力暂时告一段落（注意，仅仅是通知能力告一段落，不是接受消息的功能告一段落）。现在回到主线任务推进吧，你们继续稳步落地，不要出错，如果需要，可以做一步就执行一次全局的review评审

### 1.2 触发背景

上一轮修复解决了“重复、太长、字段堆叠”的问题，但仍然让飞书正文带着 RedCap 内部工作流的味道。再上一轮把它改成了成果摘要，但又把全局位置拿掉得太干净。随后补回的“整体任务全景图”仍然犯了层级错误：它讲的是当前飞书通知治理子任务，不是这次尚未结束的 RedCap 超长父任务线。你真正想要的是移动端快速判断：这次到底做成了什么，对我有什么用，它处在整个 RedCap 主线的哪里，主线还有什么没有结束，下一步是否还要我参与。

### 1.3 原始意图覆盖审计

| 项 | 结论 |
|---|---|
| scope_status | full-implementation |
| 已覆盖 | 飞书主段落从控制面字段改成成果摘要字段；补回父任务级、人类可读的整体任务全景图；把普通飞书回复接成安全收件箱，能被 revive/install 软扫描和 current-status 展示。 |
| 延期 | 不启用后台长连接 worker；不把普通 node-report 回复直接自动执行；不改变飞书 app/profile。 |
| 用户可见边界 | 飞书完成通知更像任务结果摘要，同时能看见 RedCap 超长主线位置；你回复飞书可被后续扫描成待处理消息，但它只会进入任务治理流程，不会绕过任务账本直接驱动 RedCap。 |

---

## 二、方案讨论

### 2.1 问题分析

飞书不是给机器看的审计页，而是给你看的“任务结果提醒”。所以正文应该少讲 RedCap 怎么过关，多讲任务完成了什么、带来了什么变化、下一步是否需要你。工程证据仍然要留，但应该放在末尾，像“可追溯记录”，而不是占据正文主位。

### 2.2 决策结果

| 问题 | 决策 | 理由 |
|---|---|---|
| 飞书主标题 | 改成成果摘要式标题 | 降低门禁、状态机、内部流程词的存在感。 |
| 整体任务全景图 | 优先从父任务账本生成 | 让你看到 RedCap 超长主线进展，而不是当前小任务的局部路线。 |
| 工程证据 | 降到“查看记录” | 保留追溯能力，但不抢正文注意力。 |
| 飞书回复指令 | 本轮只调研和记录边界 | 直接执行飞书回复会扩大权限面，必须另做安全设计。 |
| 飞书收件箱 | 做成安全入口，不做远程命令口 | 既能接住你的飞书回复，又不允许绕过中插需求、人工决策和收尾确认。 |

---

## 三、落地结果

### 3.1 完成内容

- 飞书完成通知主段落改为“这次完成了什么、带来的效果、整体任务全景图、接下来做什么、需要你做什么、查看记录”。
- “整体任务全景图”优先读取父任务账本的人类可读全景，说明超长主线目标、已完成大块、当前所在位置、剩余边界和下一步。
- 当前子任务报告的局部路线只作为 fallback，不再冒充整体全景。
- 飞书格式器会把部分内部术语转成人话，例如把 `closeout` 表达成“收尾确认”，把 `spec-check` 表达成“规范检查”。
- 沟通策略和回归检查已禁止“任务位置、阻塞状态、关键证据”等控制面标题回到飞书主段落。
- 飞书回复通道已从“只有调研边界”升级为“安全收件箱”：revive/install 可以软扫描新回复，current-status 会展示待处理数量和摘要，diagnose/spec-check 会校验它没有越权执行。

### 3.2 人话解释

以前飞书像“我通过哪些内部流程完成了某个节点”。上一版虽然像任务结果提醒了，但全景仍太窄，只讲当前小任务。现在它会先告诉你这次超长主线到底是什么、已经完成了哪些大块、现在卡在哪些边界、这次完成的小任务只是主线中的哪个位置。如果你想看细节，最后再看报告链接。

### 3.2.1 术语对照（按文件/功能解释）

| 术语 | 对应文件/功能 | 人话解释 |
|---|---|---|
| 飞书成果摘要 | `redcap-notify-format.sh` 的完成消息模板 | 飞书里优先给你看的任务结果说明。 |
| 整体任务全景图 | 飞书里的父任务线总览 | 用人话告诉你 RedCap 这次超长主线的目标、已完成大块、当前位置、剩余边界和下一步。 |
| 查看记录 | 飞书末尾的报告和最新提交摘要 | 需要追溯时再点开，不作为正文重点。 |
| 飞书回复待处理 | `feishu-notifier.py pending-scan / pending-list` | 你在飞书发来的消息可被后续会话扫描出来，供 RedCap 接着处理。 |
| 飞书安全收件箱 | `redcap-feishu-inbox.sh` | 把飞书回复展示为待处理消息；它可以提示“继续、复审、新需求”，但不会直接执行。 |
| 等待回复窗口 | `feishu-notifier.py ask / confirm / resume` | RedCap 明确向你提问时，可以短时间等待飞书回复。 |

### 3.3 飞书回复调研结论

| 问题 | 结论 |
|---|---|
| 当前能不能回复飞书来实时控制我继续任务？ | 不能稳定实时控制。普通 `notify` 只是单向通知，不会打开等待窗口。 |
| 当前有没有任何回复能力？ | 有。`ask/confirm/resume` 能在明确提问时等待回复；`pending-scan` 能在后续会话扫描新消息并放入待处理入口。 |
| 技术上能不能做成“回复飞书即进入 RedCap 待处理”？ | 已完成最小安全入口。它会进入 RedCap inbox，而不是直接执行。 |
| 能不能让回复飞书直接自动执行？ | 不建议直接执行。应先进入 RedCap 中插需求流程，由任务账本、安全边界和收尾检查接管。 |

### 3.4 已采用的后续设计

飞书回复已经按“Feishu Inbox”最小安全形态落地：

- 回复普通完成通知：进入待处理 inbox，不自动执行。
- 回复人工介入通知：可以绑定到当前等待窗口，但仍需要校验是否是当前任务需要的回答。
- 回复“继续下一步”：进入中插/续跑请求，由 RedCap 判断是否已有未完成任务和是否需要重排计划。
- 回复“这里有问题”：进入复审请求，由 RedCap 启动内部复审或棱镜复审，而不是直接回滚。

### 3.5 安全边界

- 收件箱只读取和标记飞书回复，不执行回复内容。
- revive/install 可以软扫描飞书历史；如果飞书不可用，只记录不可用，不阻塞 RedCap 主线。
- current-status 只读本地收件箱状态，不偷偷等待网络。
- open 回复必须后续进入中插需求、人工决策或当前任务合并流程，不能绕过 `.dev-task.md`、change-intake 或 closeout。

---

## 四、人工审核要点

- 当前不需要 Norven 人工介入。
- 本轮不会发送真实测试飞书；只有最终收口会发送一条真实节点汇报。
- 飞书回复指令入口不做“直接执行”，避免把聊天回复变成绕过任务治理的隐形控制通道。

---

## 五、验证结果

### 5.1 已通过验证

| 验证项 | 结果 |
|---|---|
| Bash 语法检查 | 通过 |
| Python 编译检查 | 通过 |
| 飞书格式样例渲染 | 通过 |
| human-communication-check | 通过 |
| acceptance: human-communication-check | 通过 |
| feishu-inbox-check | 通过 |
| acceptance: feishu-inbox-safe-ingress | 通过 |
| acceptance: all | 通过；过程中抓到 spec-check fixture 未补 Feishu Inbox 占位脚本，已修复并重跑全量通过 |
| Kimi 独立审查 | pass；非阻断建议（Claude 入口对齐、acceptance 缩进一致性）已吸收 |
| Claude Code 独立审查 | 进程超时无 verdict，已终止，避免外部审查本身变成时间黑洞 |

### 5.2 最终收口验证

| 验证项 | 状态 |
|---|---|
| spec-check / diagnose | 通过 |
| closeout receipt | 提交后由 `closeout-cap.sh complete` 生成 |

### 5.3 closeout runtime / receipt

| 项 | 证据 |
|---|---|
| closeout receipt | 提交后由 closeout runtime 生成；以 `./closeout-cap.sh status --task-file .dev-task.md` 输出为准 |
| 独立验收 | `acceptance_policy: not-required`；本轮使用 targeted acceptance、全量 acceptance、spec-check、diagnose 与 Kimi 独立复审收口 |

### 5.4 完成等级（禁止混报）

| 等级 | 结果 |
|---|---|
| 已实现 | 是 |
| 已自检 | 是 |
| 已独立验收 | 不要求 formal Prism quorum；已完成 Kimi 独立复审，Claude Code 超时无 verdict |
| 已正式完成 | 待提交后 closeout receipt 生成 |

---

## 六、遗留问题与下一步

| 问题 | 当前边界 | 下一步 |
|---|---|---|
| 飞书回复实时控制 | 当前不做直接执行；已完成安全收件箱 | 若未来要做实时事件订阅，需要另开任务接入平台事件，并继续保持“先入 inbox、再走任务治理” |
| 飞书术语消毒 | 已覆盖常见术语 | 后续如发现新术语污染，可继续补充替换词和检查 |
| 终端状态面 | 不在本轮范围 | 终端仍保留更多诊断字段 |

---

## 七、经验沉淀

| 维度 | 内容 |
|---|---|
| 问题源 | 把内部工作流字段压缩后直接放进飞书，虽然不再冗余，但仍让用户读到太多控制面词汇。 |
| 解决方案 | 飞书主表达改为成果摘要；整体任务全景图优先取父任务账本的人类可读摘要；工程证据只保留在末尾；机器检查禁止工作流标题和子任务局部路线冒充全景；飞书回复进入安全收件箱而不是直接执行。 |
| 最后效果 | 飞书从“内部流程摘要”进一步转向“带父任务级全局位置的任务结果提醒”；同时 RedCap 可以接住飞书回复，但回复不会越权绕过任务治理。 |

### 7.3 Evolution Factory 候选处理

- 处理：no-promote。
- 理由：本轮是既有人类沟通质量机制的继续加固，已经进入策略和回归，不需要新增重复候选。

---

## 八、附录

### 附录 A：参考资料

- 飞书开放平台：接收消息事件 `im.message.receive_v1`。  
- 飞书开放平台：消息回复接口。

### 附录 B：关键文件

- `compass/tools/redcap-notify-format.sh`
- `references/human-communication-policy.json`
- `compass/tools/redcap-human-communication-check.py`
- `compass/tools/redcap-feishu-notification-policy-check.py`
- `compass/tools/redcap-feishu-inbox.py`
- `references/feishu-inbox-policy.json`
- `compass/tools/feishu-notifier.py`

### 附录 C：Commits

```text
本报告 v1.4 记录“飞书安全收件箱”补丁、全量 acceptance 修复回归和 Kimi 复审吸收；具体提交以 git log 为准。
```
