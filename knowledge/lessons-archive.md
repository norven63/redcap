# RedCap 框架级经验归档（Archived Lessons）

> 本文件存放从 `lessons.md` 活跃层淘汰的经验条目。
> 归档条目不自动加载到 LLM 上下文，但可通过搜索找到。
> 如归档条目再次复现，应"复活"回 `lessons.md` 并 `复现次数 +1`。

---

<!-- 归档条目按移入时间倒序排列，最新归档在最上面 -->
<!-- 格式与 lessons.md 相同，额外附加"归档日期"和"归档原因" -->

### L-21: 长任务启动前必须做"目的备份"，防止上下文漂移遗忘初衷 ⚙️ 已硬化
- **场景**：E2E 验证完成后，Dispatcher 本应回到 Phase 2+4 延后任务，但在长对话中丢失了原始目的
- **根因**：LLM 在长任务中产生"注意力漂移"——完成子目标后上下文被细节填满，原始目标被稀释
- **已硬化**：state.yaml `purpose` 字段（SKILL.md §5.1 步骤 3）+ 事件循环步骤 5k「目的回读」+ state-machine.md §4 格式规范
- **经验规则**：项目初始化时将用户意图写入 `purpose` 字段（含完成标准），事件循环每轮回读
- **来源**：trpg-web E2E 后 Phase 2+4，用户手动纠偏
- **发现日期**：2026-04 | **影响度**：medium | **复现次数**：1 | **最后命中**：2026-04
- **归档日期**：2026-04 | **归档原因**：已硬化到协议层，score=0.4 < 1.0

### L-10: 跨工具指令文件必须用索引模式而非内容复制
- **场景**：为 Claude Code 和 Gemini CLI 创建系统级指令文件，直接复制了 copilot-instructions.md 规则全文，导致后续维护时需手动同步三份副本
- **根因**：未遵守"单一权威来源"原则——多份内容副本必然漂移
- **经验规则**：工具特定指令文件只写"索引 + 工具特有语法"，规则内容统一维护在 CONTRIBUTING.md；Claude Code/Gemini CLI 均支持 `@file` 原生导入
- **来源**：跨 Agent 工具兼容性实现
- **发现日期**：2026-04 | **影响度**：medium | **复现次数**：1 | **最后命中**：2026-04
- **归档日期**：2026-04 | **归档原因**：已完全落地（CLAUDE.md/GEMINI.md 均为索引），score=0.4 < 1.0

### L-3: Shell 重定向符构造文件路径时可能创建异常目录
- **场景**：Agent 执行 Shell 命令写入文件时，`>` 与绝对路径拼接，在项目根目录创建了名为 `>` 的目录
- **根因**：Shell 命令中 `>` 与路径之间缺少空格，被解析为目录名而非重定向操作符
- **经验规则**：文件写入操作必须使用 Agent 内建的 Write 工具，严禁通过 Shell 重定向 `>` / `>>` 写文件
- **来源**：TRPG-Server 实测, Step 1
- **发现日期**：2026-03 | **影响度**：medium | **复现次数**：1 | **最后命中**：2026-03
- **归档日期**：2026-04 | **归档原因**：score=0.4 < 1.0，规则已内化于 agent-adapters.md §10

### L-2: Prompt 中必须包含交付物文件清单
- **场景**：Agent 完成工作但遗漏部分交付文件，Dispatcher 无法可靠验证
- **根因**：Prompt 仅描述任务目标，未明确列出必须写入的文件列表
- **经验规则**：每个 Agent 的 Prompt 末尾附带 `## 必须写入的文件` 清单，Dispatcher 据此做交付物完整性校验（§5.7）
- **来源**：AI-Coding-Museum 冒烟测试, 多角色
- **发现日期**：2025-07 | **影响度**：medium | **复现次数**：1 | **最后命中**：2025-07
- **归档日期**：2026-04 | **归档原因**：score=0.12 < 1.0（最后命中 >12mo，recency=0.3）

### L-1: Agent 文件路径必须使用绝对路径或明确基准
- **场景**：QA Agent 将 `last-result.json` 写到项目根目录而非 `开发手册/.workflow/` 下
- **根因**：Agent 对"当前工作目录"的理解与 Dispatcher 预期不一致，相对路径基准不同
- **经验规则**：Prompt 中涉及文件读写路径时，一律使用从项目根目录起算的完整相对路径
- **来源**：AI-Coding-Museum 冒烟测试, QA Agent
- **发现日期**：2025-07 | **影响度**：medium | **复现次数**：1 | **最后命中**：2025-07
- **归档日期**：2026-04 | **归档原因**：score=0.12 < 1.0（最后命中 >12mo，recency=0.3）
