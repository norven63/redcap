# Prism 运行报告：后续治理收口（redteam）

**报告 ID**：20260421-redteam-001  
**运行 ID**：20260421-redteam-001  
**模式**：redteam  
**日期**：2026-04-21  
**主题**：formal Prism follow-up for runs lifecycle, benchmark carrier, and closeout honesty  
**触发原因**：当前任务在 repo-owned backlog 已收口后，仍保留三条独立后续线：新的 formal Prism quorum、`prism/runs` 运行残留、以及 `pending-validations.md` 中 7 项完整用户项目 E2E 队列。需要一次正式 redteam 判断这些后续线里哪些是真 blocker，哪些是口径问题，哪些可以直接转成 repo-owned 补丁。

---

## 参与 Agent

| Handle | 模型 | 角色 | injection_mode | 状态 |
|--------|------|------|----------------|------|
| `61971` | `kimi-for-coding` | challenger | prefixed | followed_up |
| `78075` | `gpt-5.4` | explorer | prefixed | responded |
| `26308` | `claude-opus-4.6` | reviewer | prefixed | ABSENT |
| `85191` | `gemini-3-flash` | historian | prefixed | responded |

> 注：challenger 的首轮输出因终端硬换行污染 JSON，Collect 阶段通过 Kimi resume 做了一次 **格式追问**，只要求重提合法 JSON，不改变结论；reviewer 本轮没有保留可复用的 Copilot session handle，且 raw 输出只有阅读轨迹没有 schema verdict，因此按 backend limitation 记为 ABSENT。

---

## Collect / quorum 结果

- 原始席位：4
- responded / followed_up：3（challenger / explorer / historian）
- absent：1（reviewer）
- N_quorum：3/4 → **达标**
- schema 口径：3 份有效结果都已落盘 `parsed.json`
- lingering `dispatched`：0

---

## Adjudicate 结果

**verdict: weak-consensus**

原因：

- 运行已达 quorum，且三路有效结果对两条核心治理线形成 3/4 共识
- reviewer 缺席，因此不升级为 consensus
- 本轮没有发现需要升级到 `deadlock` / `escalate` 的 PM Gate 冲突

---

## 共识行动（3/4 支持）

1. **为 `prism/runs` 增加 machine-readable 生命周期分类，并把 acceptance 夹具清理与 formal / named 证据保留彻底拆开。**  
   支持者：challenger + historian + explorer  
   对应 finding：`C-002`、`H-001`、`E-002`

2. **把“完整用户项目 E2E 缺载体”从抽象 backlog 变成 repo-owned benchmark carrier。**  
   支持者：challenger + historian + explorer  
   对应 finding：`C-004`、`H-004`、`E-003`

---

## 弱共识 / 单点但已采纳修复

1. **历史 formal Prism 报告不能再被口头当成可重放基线。**  
   explorer 指出两份旧报告都缺 replay-auditable 证据链；本轮采纳的落点不是伪造旧 run，而是把 `index.yaml` / `current-status` 的口径收紧为“`archived: true` 才是 replay-auditable baseline”。

2. **`baton-launcher.sh` 需要补齐 Codex CLI 接入。**  
   challenger 单点指出当前 launcher 仍缺 `codex` 分支；本轮直接补齐 `codex exec` 的 stdin 输入与 `--output-last-message` 结果通道。

3. **当前 run 不应继续被描述成“quorum=0”。**  
   explorer 指出“已启动但未 Collect 终态”和“从未启动”是两种不同状态；本轮已通过 Collect 把 run 收到 `3 responded/followed_up + 1 absent` 的真实终态。

---

## ABSENT 说明

- `reviewer / claude-opus-4.6`：本轮 shell run 退出时没有形成 schema verdict；raw 输出仅包含阅读轨迹和 stats，且当前 run 未保留可复用的 Copilot session id / follow-up 模板，按 backend limitation 记为 ABSENT。

---

## OPEN_QUESTIONS

1. `prism/runs` 的 acceptance 清理虽然现在可以安全限定到 `acceptance-fixture`，但 formal run 与 named/manual run 仍默认 preserve；未来若要进一步瘦身，需要单独的 retention 决策。
2. benchmark carrier 已解决“没有载体”的 blocker，但 7 项 pending-validations 仍需要单独的完整用户项目 E2E tranche 去真正消费。
3. Synthesis Audit 机器门禁仍未形成和 quorum 同等级的脚本化证明，本轮只做了 Collect / Archive 的物理闭环。

---

## 本轮落地修补

- `prism/tools/prism-runs-lifecycle.sh`
- `prism/tools/prism-evidence-check.sh`
- `prism/protocol.md`
- `compass/tools/redcap-current-status.sh`
- `compass/tools/redcap-diagnose.sh`
- `compass/tools/baton-launcher.sh`
- `loom/tools/redcap-e2e-benchmark-carrier.sh`
- `loom/fixtures/md-table-tool-benchmark/`
- `loom/test-reports/benchmark-scenario.md`
- `loom/test-reports/pending-validations.md`
- `compass/knowledge/lessons.md`

---

## 运行结论

本轮 formal Prism 证明：当前真正卡住后续治理的，不是“没有更多报告”，而是三件更具体的事：

1. `prism/runs` 之前没有 machine-readable lifecycle，导致清理动作即使拿到批准也缺少安全判定面。
2. `pending-validations` 之前缺的是 benchmark carrier，而不是更多纸面说明。
3. 历史 formal 报告数量不等于 replay-auditable baseline，必须把 `archived=true` 和 legacy 索引口径拆开。

因此，本轮最重要的结果不是“又多了一份报告”，而是把这三条后续线都转成了更诚实、可执行、可继续验证的 repo-owned 机制。
