# Runtime Prism Tools Facades

This directory is the package-visible compatibility surface for Prism support.
The authoritative source remains `prism/README.md` and `prism/tools/*`.

These files are intentionally thin. They exist so the alpha runtime package can
expose Prism readiness/provider diagnostics without moving, deleting, or
replacing the historical Prism anchors.

