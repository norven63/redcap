# Codex Host Adapter Boundary

Codex still imports root `AGENTS.md` from the managed workspace.

This directory records the package-visible host-adapter boundary only. It is not yet a replacement for root startup imports, and it must not be used to claim Codex has repo-owned reply-veto hooks.
