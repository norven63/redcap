# RedCap Fixtures

This directory stores deterministic, portable fixtures used by RedCap contract
checks and installed-package self-checks.

Fixtures are not runtime evidence, task ledgers, user state, or private
identity state. Live runtime evidence belongs under the active project
`.redcap/evidence/`. Source `assets/evidence/` remains limited to boundary
files and ignore policy.
