# Prism Fixtures

This directory stores portable Prism fixtures used to verify provider evidence
parsing, raw metadata integrity, session continuity, and installed-package
self-check behavior.

These fixtures are bounded contract inputs. They must not be treated as live
Prism run evidence.
