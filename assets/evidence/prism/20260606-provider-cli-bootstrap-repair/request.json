{
  "task": "Bootstrap verify Prism provider CLI invocation",
  "user_intent": "Norven explicitly authorized a narrow bootstrap repair so Prism can call Kimi and Claude Code again.",
  "main_claim": "The provider command should return a valid Prism review without using tools, editing files, or entering an unbounded interactive planning flow.",
  "changed_reality": [
    "Kimi must run in non-interactive print mode.",
    "Claude Code must run in bare mode with structured JSON output constraints."
  ],
  "review_mode": "provider invocation smoke test",
  "risk_level": "low",
  "requested_providers": [
    "kimi",
    "claude-code"
  ],
  "proposal": [
    "Return a minimal valid Prism review for this smoke test.",
    "Do not inspect files.",
    "Do not call tools.",
    "Do not propose broad implementation work."
  ],
  "non_goals": [
    "Do not review the full Chinese-first policy task here.",
    "Do not review timeout follow-up implementation here."
  ],
  "review_questions": [
    "Can this provider return the required Prism review shape through the dispatcher?"
  ],
  "expected_output": "Return strict JSON with provider, verdict, confidence, reality_delta, main_concern, top_risks, missing_evidence, minimum_fix, anti_loop_signal, and user_intent_alignment."
}
