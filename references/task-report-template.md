# RedCap 任务完成报告模板

> **用途**：每次 RedCap 框架自身开发任务完成后的标准回报格式。
> **归档位置**：Layer B 任务统一归档到 `compass/docs/task-reports/YYYY-MM-DD-<topic>.md`；`prism/reports/` 仅存放 Prism 模式报告，**不能替代任务完成报告**
> **触发时机**：完成任务级 Review Gate 后、结束任务前
> **机器审计**：SessionEnd Hook 会检查物理报告文件；已提交的报告自动可见，未提交但已暂存的报告需先执行 `bash compass/tools/redcap-task-report-register.sh <claude|gemini|copilot> <report_path>` 进行显式登记。`redcap-task-report-check.sh` 会继续调用 `redcap-human-output-quality-check.sh` 审计“四句先看懂”、术语对照、完成等级与 receipt 证据是否自洽。Layer B 终态统一收口优先走 `./closeout-cap.sh` / `redcap-layerb-closeout-runtime.sh`，由 closeout runtime 核对承诺账本、Prism 独立验收并生成 receipt / summary。只在对话里口头汇报、或只留在工作区草稿中，仍视为未归档
>
> **完成态红线**：棱镜已成为 completed 的默认独立验收前置门；receipt 是唯一正式完工凭证；作者不能单独完成 closeout，也不得单独宣称 completed。

---

## 报告格式

---

# 任务完成报告：{任务名称}

**报告日期**：{YYYY-MM-DD}
**执行者**：Cap（{使用的主力 Agent CLI + 模型}）
**报告版本**：v1.0

---

## 零、先看懂当前局面

> **这四段会被状态汇报、收尾摘要与飞书通知优先抽取。** 即使完整报告很长，这里也必须让 Norven 在 15-30 秒内先看懂：现在已经做到哪里、上一刀是什么、下一刀是什么、整条路线卡在哪个位置。

### 0.1 当前已完成

- 当前已完成：{一句话概括本轮已经落地的结果}
- 详情：{2-4 句话说明真正做成了什么、涉及哪些关键文件/功能}

### 0.2 上一步完成的是

- 上一步完成的是：{上一刀已收口的内容，以及它如何把任务推进到当前这一步}

### 0.3 下一步计划做的是

- 下一步计划做的是：{下一刀最优先继续推进的事项；若本轮已经整体完成，则写“无”}

### 0.4 整体计划脉络图与当前位置

- 整体计划脉络图是：{用 1 行串起阶段顺序或主线脉络}
- 当前所在位置：{明确写出当前所处阶段 / backlog item / active slice}

---

## 一、需求背景

### 1.1 原始问题（用户原文，禁止改写）

> {从 .dev-task.md §原始输入 或 explore-notes.md 直接拷贝用户原文，逐 Q 完整展示}

### 1.2 触发背景

{2-4 句话描述：这些需求是在什么情况下提出的？解决了什么根本问题？为什么现在做它？}

---

## 二、方案讨论

### 2.1 问题分析

{对每个 Q，描述问题的本质和复杂度。可引用 explore-notes.md 中的演进记录}

### 2.2 方案选项

| Q | 选项 | 描述 | 优点 | 缺点 |
|---|------|------|------|------|
| Q1 | 选项 A | ... | ... | ... |
| Q1 | 选项 B | ... | ... | ... |

### 2.3 决策结果

| Q | 采纳方案 | 决策理由 | 决策方 |
|---|---------|---------|------|
| Q1 | 选项 A | {核心理由，1-2句} | CAP_DECIDE / NORVEN_DECIDE |

---

## 三、落地结果

### 3.1 变更文件清单

| 文件 | 变更类型 | 变更摘要 |
|------|---------|---------|
| `{path}` | 新建 / 修改 / 删除 | {1行描述} |

### 3.2 技术实现要点

{对每个重要技术决策，用 2-3 句话描述实现细节和设计理由。不需要贴代码，只需要说清楚“做了什么”和“为什么这么做”。若出现此前未共同约定的内部术语、缩写、链路名或阶段名，必须顺手解释它对应哪个文件/功能、做了什么、为什么重要。}

### 3.2.1 术语对照（按文件/功能解释）

| 术语 | 对应文件/功能 | 人话解释 |
|------|--------------|---------|
| {术语} | `{path}` / {功能名} | {这个术语在本次任务里到底指什么、为什么重要} |

### 3.3 关联变更

{列出因 §6 联动规则触发的次级修改，以及修改原因}

---

## 四、人工审核要点

> ⚠️ 以下是 Norven 需要重点确认的内容，其他部分 Cap 已自行验证。

| 序号 | 审核项 | 说明 | 优先级 |
|------|-------|------|------|
| 1 | {审核项} | {为什么需要人工判断，而不是 Cap 自行决策} | P0 / P1 / P2 |

---

## 五、验证结果

### 5.1 自动化验证

| 验证项 | 命令 | 结果 |
|--------|------|------|
| 语法检查 | `bash -n {script}` | ✅ / ❌ |
| 引用一致性 | `{grep命令}` | ✅ / ❌ |
| E2E 测试 | `{命令}` | ✅ / ❌ |

### 5.2 人工验证项（Cap 无法自动化验证的）

- [ ] {需要 Norven 手动测试的项目}
- [ ] {需要观察实际运行效果的项目}

### 5.3 closeout runtime / receipt

| 项目 | 结果 |
|------|------|
| 执行承诺账本 | {已清 / 仍有未清项} |
| 棱镜验收 | {通过 / 未通过 / 未执行} |
| closeout summary | `{path 或 无}` |
| closeout receipt | `{path 或 无}` |
| rescue audit（如有） | `{path 或 无}` |

### 5.4 完成等级（禁止混报）

| 等级 | 结果 |
|------|------|
| 已实现 | {是 / 否} |
| 已自检 | {是 / 否} |
| 已独立验收 | {是 / 否；默认以棱镜为准} |
| 已正式完成 | {是 / 否；receipt 是唯一正式完工凭证} |

---

## 六、遗留问题与下一步

### 6.1 本次未处理的问题

| 问题 | 原因 | 建议优先级 |
|------|------|----------|
| {问题描述} | {为什么本次不处理} | P0 / P1 / P2 |

### 6.2 触发的新问题

{执行过程中发现的、不在本次任务范围内的问题，记录到 lessons.md 或下次任务待办}

### 6.3 推荐的下一步行动

1. {下一步 1}
2. {下一步 2}

---

## 七、经验沉淀

### 7.1 新增 Lesson（建议写入 knowledge/lessons.md）

| 编号 | 标题 | 核心内容 |
|------|------|---------|
| L-{n} | {标题} | {1句话总结} |

### 7.2 流程改进建议

{本次执行中发现的流程改进点，可提议修改 CONTRIBUTING.md 或 SKILL.md}

---

## 八、附录

### 附录 A：Commits

```
{git log --oneline 相关的 commit 列表}
```

### 附录 B：棱镜调用记录（如有）

| 模式 | 问题 | 结论 | 报告路径 |
|------|------|------|---------|
| {council/explore/redteam} | {问题} | {1句话结论} | `prism/reports/...` |

### 附录 C：相关文档索引

- 需求原始记录：{`.dev-task.md` 或 `explore-notes.md` 相关条目}
- 设计文档：{design doc 路径（如有）}
- 变更影响分析：{§6 联动分析表}
